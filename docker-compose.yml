version: "3"

services:
  web:
    build:
      context: .
      args:
        env: production
        port: 8000
    command: yarn run start
    volumes:
      - ./dist:/app/dist
    ports:
      - "8000:8000"